# Inf-Dir 对照 Files 功能路线图

本文记录 Inf-Dir 与 Files 的功能差距、优先级和验收标准。它用于规划后续
迭代，不要求机械复刻 Files；Inf-Dir 仍以 Hyprland Dwindle 风格动态分割、多 workspace 和
独立进程 Viewer 插件体系为核心差异。

## 1. 对照基线

- Files 参考源码：`ai_refs/files`
- Files commit：`c4a4ff95b413b8ea3d0f04fc13ef99512bf135cc`
- 基线日期：2026-07-21
- Inf-Dir 基线：`cb52349`

更新 Files submodule 后，应先更新本节 commit，再重新核对新增功能，避免
路线图随上游变化失去可复现性。

## 2. 已有能力

以下能力已经存在，不属于本路线图的待办范围：

- Hyprland Dwindle 风格任意水平/垂直分割、关闭、缩放、面板交换和多 workspace；
- 每面板独立路径、标签页、历史、排序、分组、过滤和视图模式；
- 文件与正文搜索（fd/ripgrep 插件）；
- manifest 驱动的独立进程 Quick View Viewer；
- 新建、复制、剪切、粘贴、移动、删除、重命名和创建快捷方式；
- Windows Shell 右键菜单、打开方式、属性和 Shell New 集成；
- Home、最近文件、收藏、快速访问、驱动器和回收站浏览；
- CfAPI 云盘发现与同步状态显示；
- 明暗主题、隐藏文件和文件扩展名开关。

## 3. 规划原则

1. 先修复可见但无响应的命令，再增加新入口。
2. 先补齐文件操作的可靠性，再扩展 Git、标签等增强能力。
3. Windows Shell 能力可以作为 fallback，但核心工作流应有一致的应用内入口。
4. 长耗时文件操作不得阻塞 Flutter UI isolate。
5. 文件系统与 Shell 集成继续使用 Win32 / COM / Shell FFI，不引入主进程第三方 DLL。
6. 新 UI 必须使用 `app_theme.dart` token，并同时覆盖明暗主题。
7. 每项功能完成时必须包含失败路径、取消/重入行为和相应测试。

## 4. P0：产品完整性与可靠性

### 4.1 清理无响应菜单

当前 `file_pane.dart` 中以下命令传入了空回调，用户可以点击但没有结果：

- [x] 实现“在新窗口中打开”，或在多窗口功能完成前隐藏该菜单项。
- [x] 实现“发送到”，或先隐藏该菜单项。
- [x] 实现“固定到侧边栏”，并提供对应的取消固定入口；或先隐藏该菜单项。
- [x] 检查全部菜单，禁止以非空 callback 表示未实现功能。
- [x] 为菜单 enable/visible 状态增加 FilePane widget 集成测试（本轮已补 builder 单测）。

验收标准：界面中不存在点击后无反馈、无动作的命令。

### 4.2 补齐应用菜单

顶部应用菜单已接入统一命令模型；尚未实现的能力继续保留在路线图中，不展示为无响应菜单项。

- [x] 文件菜单：新建标签页、关闭标签页、关闭面板、退出；新窗口（多窗口已搁置，暂不提供）。
- [x] 编辑菜单：剪切、复制、粘贴、全选、反选。
- [x] 编辑菜单：Undo、Redo（复用应用级文件操作历史）。
- [x] 收藏夹菜单：添加收藏、移除收藏、管理收藏。
- [x] 信息菜单：关于和版本。
- [ ] 信息菜单：问题反馈入口（待确定反馈渠道）。
- [x] 所有菜单命令复用统一 `CommandMenuItem`，不复制 FilePane 业务逻辑。
- [x] 命令可用性随焦点面板、选择项和剪贴板状态更新。

### 4.3 回收站完整工作流

目前只能枚举回收站，应用主动禁用了项目打开行为。

- [x] 恢复选中项目到原始位置。
- [x] 原始位置不存在时提供明确处理方式（选择其他位置或还原到桌面）。
- [x] 原始位置同名冲突时提供明确处理方式（替换 / 保留两者 / 跳过）。
- [x] 全部恢复。
- [x] 永久删除选中项目。
- [x] 清空回收站，并显示不可撤销确认。
- [x] 操作完成后刷新回收站面板和已打开的原始目录。
- [x] 还原已接入文件任务中心：worker thread 执行枚举与还原，任务中心显示
  “还原 N 个项目”任务（进度、取消、逐项失败明细与 HRESULT）。
- [ ] 覆盖取消、失败、空回收站和多选测试（已补还原目标规划、冲突、空操作和多选单测，对话框与原生交互待覆盖）。

验收标准：用户不需要打开 Explorer 即可完成常见回收站操作。

### 4.4 文件任务中心

当前复制、移动和删除使用静默 `IFileOperation`，没有进度、取消、冲突处理
和失败明细；同步 FFI 还可能阻塞 UI isolate。

- [x] 建立统一 `FileOperationTask` 模型：类型、来源、目标、状态和结果。
- [x] 原生层通过 `IFileOperationProgressSink` 或等效机制报告进度。
- [x] 文件操作移出 UI isolate，Flutter 侧只消费任务事件。
- [x] 提供任务中心：排队、进行中、成功、失败和已取消。
- [x] 支持取消尚未完成的任务。
- [x] 支持同名冲突：替换、跳过、保留两者，并可应用到全部。
- [x] 展示可定位到具体路径的失败原因。
- [x] 操作完成后只增量刷新受影响的面板和目录 cache。
- [x] 应用关闭时处理进行中任务，禁止静默中断。

实施说明：

- 原生层以 worker thread 执行 `IFileOperation`，并通过 `Advise` 的
  `IFileOperationProgressSink` 上报真实百分比进度（`UpdateProgress`）与
  逐文件结果（`PostCopyItem/PostMoveItem/PostDeleteItem` 的路径 + HRESULT）；
  终态后经 `InfDirGetFileOperationResultsW`（UTF-8 JSON）取回并
  `InfDirCloseFileOperationW` 释放状态，Flutter 侧经 `FileOperationCenter`
  串行排队并消费任务事件，粘贴 / 删除 / 回收站还原等入口已接入。
  （还原另有独立的 `InfDirStartRestoreOperationW`，枚举与移动同 worker。）
- 任务中心 UI 位于顶栏常驻入口（活动任务数角标，空闲时为 0）与主区右下角
  flyout：展示状态、进度、目标、失败信息，支持取消、单条移除与清除已完成；
  失败任务列出具体失败路径及 HRESULT（最多 2 条 + 汇总），部分失败的任务
  显示“已完成，N 项失败”。
- 运行中取消是尽力而为：`FOF_SILENT` 的 `PerformOperations` 无法中途
  打断，取消标志在当前批返回后生效并标记任务为已取消。
- 复制 / 移动在目标存在同名项时先弹出冲突对话框：替换（默认覆盖）、跳过
  （源列表剔除）、保留两者（`FOF_RENAMEONCOLLISION`，Shell 自动改名），
  并支持“应用到全部”；策略按组执行，进度按组加权聚合到同一任务。保留
  两者时目标面板整目录刷新（新名称由 Shell 决定）。
- 操作完成后增量刷新：`DirectoryRepository.patchCompleteCache` 就地补丁
  受影响目录的缓存（新增条目经 `InfDirBuildNameSortKeyW` 带上与枚举一致
  的自然排序键），`LayoutState.applyLocalChanges` 扇形更新所有正显示受影响
  目录的面板，侧栏经 repository 回调重读；只有目标名未知（保留两者）或
  目录正在枚举时才退化为定点失效。
- 应用关闭时若存在进行中/排队中的任务，先弹「文件操作正在进行」确认
  （取消 / 仍然关闭），不允许静默中断；取消后窗口保持打开并可重试关闭。

验收标准：大文件、跨盘和批量操作期间 UI 保持响应，用户可以看到结果并
处理冲突。

### 4.5 Undo / Redo

底层设置了 `FOF_ALLOWUNDO`，但应用没有可操作的历史栈。

- [x] 定义应用级文件操作历史及可撤销边界。
- [x] 支持复制、移动、重命名、新建和回收站删除的 Undo。
- [x] 支持 Redo，并在新操作后正确截断 redo 栈。
- [x] 接入 `Ctrl+Z`、`Ctrl+Y` 和编辑菜单。
- [x] 不可撤销操作必须在执行前明确提示。
- [x] 撤销后同步更新所有展示相关路径的面板。

实施说明：

- `FileOperationHistoryStack` 记录类型 + 来源 + 目标（新操作截断 redo
  尾巴，`replaceCurrent` 刷新当前记录）；粘贴 / 删除 / 还原 / 重命名 /
  新建成功后录制，永久删除不入栈（确认对话框已声明不可撤销）。
- 原生 sink 在 `PostCopy/PostMove/PostDeleteItem` 捕获 `psiNewlyCreated`：
  `createdPath`（含"保留两者"的实际改名）与 `recycledPath`（回收站 $R
  解析名）随逐项结果返回，历史记录无需枚举回收站即可还原/重删，redo
  后自动刷新新 ID（对应 Files 的 `ModifyCurrentHistory`）。
- `UndoRedoService` 反向操作表：复制→永久删除副本（确认对话框）、
  移动→移回、重命名→改回、新建→删除、回收站删除→还原、还原→再入
  回收站；全部经 `FileOperationCenter` 排队（进度/取消/逐项明细复用
  任务中心），完成后走增量刷新（面板扇形更新 + 目录缓存补丁）。

### 4.6 文件拖放

当前产品范围以应用内 pane 当前目录与文件夹行拖放为完成标准。标签页、
侧边栏、workspace 目标以及 Explorer 外部拖入/拖出保留为可选扩展，
不阻塞 M2。

- [x] 面板内及跨面板拖动文件到文件夹。
- [x] 跨面板复制/移动：同盘默认移动、跨盘默认复制，Ctrl 强制复制、Shift
  强制移动；多选作为同一任务执行。
- [ ] 拖到标签页、侧边栏目录和 workspace 面板（可选扩展）。
- [ ] 接收 Explorer 等外部进程拖入的文件（可选扩展）。
- [ ] 将文件拖出到 Explorer 或其他支持 CF_HDROP 的应用（可选扩展）。
- [x] 显示合法目标、拒绝目标和复制/移动意图反馈。
- [x] 防止拖回源目录、目录自身/后代和虚拟位置；权限或只读
  目标失败通过文件任务中心展示具体错误。

实施说明：

- 文件列表各视图模式统一产生 `FileDragPayload`，仅常规文件系统位置可作为
  拖动来源；目标 pane 内容区接受投放到当前目录，目录行接受投放到对应
  子目录，同 pane 与跨 pane 使用相同判定和执行链路。
- `FileDropService` 负责纯判定，投放执行复用粘贴的冲突对话框、
  `FileOperationCenter`、Undo/Redo 历史和增量刷新，不维护第二套文件操作链路。

### 4.7 接通详情与预览面板

- [x] 增加详情面板入口并渲染名称、类型、大小、时间、路径等元数据。
- [x] 多选时显示选择数量与总大小。
- [x] 增加预览面板入口，并复用 Quick View 插件解析规则。
- [x] 详情与预览互斥逻辑与现有 Controller 状态一致。
- [x] 面板可调整宽度，且每个 pane 独立保存状态。
- [x] 无选择、不支持预览、加载中和 Viewer 失败均有明确状态。

实施说明：

- 产品决策：不实现 Files 式内嵌详情/预览侧栏。F3 Quick View（独立进程
  Viewer 弹窗）已覆盖预览与文件信息查看，入口为 F3 快捷键与右键「快速查看」。
- Viewer 复用 manifest 解析规则；文件夹、无候选、插件缺失和启动失败均有
  toast 反馈。多选时取焦点项 / 首个选中项打开。
- `PaneController` / 会话快照仍保留 `showDetailsPane`、`showPreviewPane`
  字段，但当前不渲染对应 UI；内嵌面板与每 pane 独立宽度不再作为目标。

## 5. P1：核心体验补齐

### 5.1 文件缩略图

- [x] 图片、视频、PDF 和 Office 文件优先显示 Shell thumbnail。
- [x] 无缩略图或失败时回退到现有文件图标。
- [x] 只为可见项加载，滚出视口后允许取消低优先级任务。
- [x] 建立有界内存/磁盘缓存，缓存键包含路径、mtime 和请求尺寸。
- [x] 提供“显示缩略图”设置和清理缓存入口。
- [x] 防止大量图片目录造成滚动卡顿或无界内存增长。

实施说明：

- 原生 `GetFileImagePngW` 去掉强制 `SIIGBF_ICONONLY`；大图标 / 平铺 / 内容
  视图（逻辑尺寸 ≥ 34）请求 `SIIGBF_THUMBNAILONLY`，先查 Shell 缓存再提取。
  详细信息 / 列表 / 小图标 / 紧凑仍走 `GetFileIconPngW` 类型图标。
- 提取在独立 isolate 上执行，列表只为已构建（可见）单元格请求；滚出视口后
  丢弃过期结果。失败回退现有图标并不再重试同一 cache key。
- 缓存键为 `T:path:mtimeMs:size`；内存 LRU 256 项 / 32MB，磁盘位于
  `%LOCALAPPDATA%/Inf-Dir/thumbs/`。
- 「选项 → 显示缩略图」默认开启并写入 `prefs.json`；「清除缩略图缓存」清空
  内存与磁盘。

### 5.2 压缩与解压

- [x] 实现 ZIP 解压到当前目录、同名子目录和指定目录。
- [x] 补齐 7z 创建：通过 `plugins/archive` 的独立 7-Zip 操作插件执行。
- [x] 提供统一“创建压缩包”对话框。
- [x] 评估并落地 7z 独立进程实现；RAR、tar、gzip 后续按格式能力扩展。
- [x] 支持密码、编码、覆盖策略和损坏包错误提示。
- [x] 压缩/解压纳入文件任务中心。

实施说明：

- 压缩与解压统一走 `ArchiveService`（随插件分发的 `7za.exe` 子进程），
  `createArchive` 用 `a -t<fmt> -mx<n> [-p<密码>] [-mhe=on]`，`extractArchive`
  用 `x -o<目录> [-ao{覆盖/跳过/保留两者}] [-mcp=<编码>] [-p<密码>]`。
- 生产环境以 `Process.start` + `-bsp1` 流式解析百分比进度，`onProgress`
  直接喂给任务中心 `FileOperationTask.updateProgress`；`cancelRequested` 触发
  `Process.kill`，与 Shell 文件操作同一套取消语义。损坏包 / 密码错误等非零
  退出码连同 stderr 的 `ERROR:` 行封装进 `ArchiveException` 在任务中心展示。
- 右键菜单：压缩子菜单新增“创建压缩包…”（名称 / 格式 / 压缩级别 / 密码 /
  加密文件名），对归档文件新增“解压”子菜单（解压文件… / 解压到当前文件夹 /
  解压到 <目录>）；解压对话框含目标文件夹（可浏览）、覆盖策略、密码、ZIP
  名称编码与“完成后打开目标文件夹”。
- 归档判定以扩展名为准（`.7z/.zip/.rar/.tar/.gz/.tgz/.bz2/.tbz2/.xz/.txz/
  .lzh/.cab/.iso/.jar/.wim/.zst`），对齐 Files 的解压入口范围；创建仅支持
  ZIP 与 7z，其余格式（RAR/tar/gzip）留待按格式能力扩展。
- 命名与冲突对齐 Files：创建目标已存在时追加 `" (2)"`、`" (3)"`（绝不覆盖/
  合并进已有压缩包），"解压到 <目录>" 同样生成唯一同级目录；创建失败时删除
  残留的部分压缩包。
- `plugins/archive/7zip/plugin.json` 的 `capabilities.archive.operations`
  更新为 `["create", "extract"]`。

### 5.3 多窗口（已搁置）

> 决策：Inf-Dir 的 Dwindle 风格动态分割 + 多 pane + 多 workspace 已覆盖"同时操作多个位置"
> 的核心需求，多窗口增量价值有限、且引入原生窗口层/会话并发复杂度，暂不实现。
> 若未来需要（多显示器、跨窗口拖放）再重新评估。

- ~~定义新窗口启动协议和安全的路径参数传递。~~
- ~~从目录、Home、侧边栏和搜索结果在新窗口打开。~~
- ~~每个窗口拥有独立 LayoutState 和焦点状态。~~
- ~~明确会话恢复策略：仅主窗口或恢复全部窗口。~~
- ~~避免多个窗口并发覆盖同一会话 JSON。~~

### 5.4 高级标签页管理

- [x] 拖动排序标签页。
- [x] 在 pane 之间移动或复制标签页。
- [x] 复制当前标签页。
- [x] 恢复最近关闭的标签页。
- [x] 关闭其他、左侧、右侧和全部标签页。
- [x] 标签页各自保存前进/后退历史，而不是切换时清空 pane 历史。
- [x] 补齐 `Ctrl+T`、`Ctrl+W`、`Ctrl+Tab` 等快捷键。

实施说明：

- 每个标签携带独立 back/forward 栈（`TabInfo`）：PaneController 保留工作态
  双栈，`_flushActiveTab` 在切换/关闭/序列化/移交前统一写回，切换标签不再
  清历史；新标签页从空历史开始。`TabInfo` 的 `==` 只比较 path+label，
  path 变化替换实例以触发标签栏 Selector rebuild，纯栈变化不触发。
- 新操作：`moveTab`（拖动排序）、`duplicateTab`（深拷贝历史）、
  `insertTab`/`takeTab`（恢复与跨 pane 移交）、`cycleTab`（环绕切换）、
  `closeOtherTabs`/`closeTabsToTheLeft`/`closeTabsToTheRight`、
  `collectClosedRecords`；pane 至少保留一个标签（`closeTab`/`takeTab` 拒绝
  取走最后一个）。
- 「最近关闭的标签页」为会话级 LIFO（上限 20，不落盘）：关闭标签、批量
  关闭、关闭 pane / workspace 均记录（pane 多标签逆序压栈，连续恢复按原
  顺序返回）；`Ctrl+Shift+T` 恢复到焦点 pane 的原索引（越界 clamp）。
- 跨 pane 拖放：拖动数据只带 paneId+索引，落点时经 LayoutState 现取状态；
  无修饰键=移动、Ctrl=复制；pane 的唯一标签不挂拖动（无处可去，拖动无
  反应），落点层仍兜底拒绝取走源 pane 唯一标签的移动，拒绝时标签栏显示
  danger 指示。
- 标签栏交互：右键菜单（新建/复制/关闭/关闭其他/关闭左侧/关闭右侧/
  关闭全部/恢复最近关闭，复用 `CommandMenuItem` 与 `showCommandMenu`）、
  中键关闭、拖动插入指示条。「关闭全部」在 pane 数大于 1 时关闭整个
  pane（全部标签入最近关闭栈），最后一个 pane 降级为「关闭其他」。
- 快捷键走全局 raw handler（先于焦点遍历派发，地址栏持焦也生效）：
  `Ctrl+T` 新建、`Ctrl+W` 关闭（单标签且多 pane 时关 pane）、
  `Ctrl+Shift+T` 恢复、`Ctrl+Tab`/`Ctrl+Shift+Tab` 环绕切换。
- 会话快照升至 schema v2：`PaneLayoutSnapshot.tabs` 为
  `{path, backStack, forwardStack}` 对象数组；读取兼容 v1（字符串数组 →
  空栈，pane 级历史归入活动标签）。旧版二进制读 v2 缓存将回落默认布局
  （store 已有 .bak 回退）。

### 5.5 网络位置

- [ ] 支持 UNC 路径作为一等地址栏和标签页路径。
- [ ] 侧边栏显示已映射网络驱动器与网络位置。
- [ ] 添加/删除网络位置与映射/断开网络驱动器。
- [ ] 提供网络发现关闭、认证失败和离线状态。
- [ ] 评估 FTP/SFTP 是否属于核心能力；若实现，必须隔离凭据存储。

### 5.6 设置中心

- [ ] 建立统一设置页和可搜索的设置分类。
- [ ] 持久化隐藏文件、文件扩展名、主题和默认视图等偏好。
- [ ] 提供删除确认策略、单击/双击打开行为和默认新标签路径。
- [ ] 提供缩略图、文件夹大小和网络位置开关。
- [ ] 保留 Viewer 管理，但将其纳入设置中心。
- [ ] 为设置 schema 增加版本与迁移策略。

### 5.7 文件夹大小

- [ ] 提供可关闭的后台目录大小计算。
- [ ] 仅计算可见项并限制并发。
- [ ] 支持取消、权限不足、链接循环和超大目录。
- [ ] 在详情视图、状态栏和详情面板中共享结果缓存。

## 6. P2：增强能力

以下功能应在 P0/P1 稳定后按用户需求选择实现：

- [ ] 文件标签：自定义名称、颜色、排序、筛选和按标签聚合。
- [ ] Windows 分享面板。
- [ ] Git 状态列及 clone/init/fetch/pull/push/sync。
- [ ] 命令面板与可自定义快捷键。
- [ ] 工具栏自定义和命令显隐/排序。
- [ ] 图片旋转以及设置壁纸、锁屏和应用背景。
- [ ] Pin to Start、管理员运行和以其他用户运行。
- [ ] 字体、证书和 INF 驱动安装入口。
- [ ] 扩展属性、权限、安全信息和文件哈希。
- [ ] 全屏、Compact Overlay 和更完整的外观配置。

## 7. 推荐实施顺序

依赖关系如下：

```text
占位命令清理
    |
    +--> 回收站工作流
    |
    +--> 文件任务中心 --> Undo/Redo --> 文件拖放
    |
    +--> 详情/预览（F3 Quick View，已完成） --> 缩略图
    |
    +--> 高级标签页（多窗口已搁置）
    |
    +--> 设置中心 --> 网络/缩略图/文件夹大小偏好
```

建议里程碑：

1. M1 完整性：清理空回调、补顶部菜单、完成回收站。
2. M2 操作可靠性：文件任务中心、冲突处理、Undo/Redo、拖放。
3. M3 浏览体验：缩略图、压缩与解压（详情/预览已由 F3 Quick View 覆盖）。
4. M4 导航能力：高级标签页、网络位置和设置中心。
5. M5 增强生态：标签、分享、Git 和自定义命令。

## 8. 通用完成标准

每个复选项只有同时满足以下条件才可标记完成：

- 功能入口、成功路径、失败路径和取消路径完整；
- 不阻塞 UI isolate，不造成窗口无响应；
- 多 pane、多 workspace 和多标签场景行为一致；
- 文件变更后目录 cache、选择状态和侧边栏正确同步；
- 支持路径空格、Unicode、长路径、只读目录和权限错误；
- 明暗主题、键盘操作和高 DPI 下无布局问题；
- Dart 单元/widget 测试与必要的 Windows 原生测试通过；
- `flutter analyze` 和 `flutter test` 通过；
- 相关架构或协议变化同步更新 `docs/`。
